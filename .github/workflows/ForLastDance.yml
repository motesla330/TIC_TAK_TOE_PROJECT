name: CI for SessionManagement

on:
  push:
    paths:
      - 'SessionManagement.cpp'
      - 'Headers/SessionManagement.h'

jobs:
  cpplint-check:
    name: Run cpplint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install cpplint
        run: pip install cpplint

      - name: Run cpplint on SessionManagement.cpp
        run: cpplint SessionManagement.cpp

      - name: Run cpplint on Headers/SessionManagement.h
        run: cpplint Headers/SessionManagement.h
